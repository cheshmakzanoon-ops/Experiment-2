# Studio verification follow-through — 19 September 2026

## Published run 67 — corrective follow-through

`db596080afc8869537df7488550cbba44059197b` triggered run `35476665563`.
The JVM-test step passed. Static analysis failed on ktlint formatting in
`BrushAttributeUiTest.kt`. Every device lane reached instrumentation and reported the same
`BrushStudioUiTest.selectingAndUsingAPresetPublishesExactlyOnce` failure: `Library` reported
`didOverflowWidth=true` at a measured 46–89 px width. Commit `9a0e3455` makes each tab label fill
the width already allocated by `TabRow` and reformats the rejected test chains without changing
their assertions.

The two API 35/16 KB jobs also failed in different `SelectionGestureDeviceTest` methods and then
terminated natively. Their preserved tombstones enter JIT-compiled Compose startup code rather than
selection code: one includes `Recomposer.addRunning`; the independent repeat includes
`SubcomposeLayout`, `Scaffold` and `GalleryScreen`. The selection suite was unnecessarily
launching the production Compose `MainActivity` before replacing its hierarchy with
`ArtFlowCanvasView`.

The corrective harness uses a debug-only `@AndroidEntryPoint` `ComponentActivity` with a plain
`FrameLayout`. The production Hilt-injected `ArtFlowCanvasView`, repository, GL attachment, real
MotionEvents, commit-boundary checks, concurrency checks and all assertions remain. No lane, JIT
setting, test, assertion or failure path is disabled. A new exact-commit run must determine what
remains.

Base: `019648ea501c8de5f3d0af04fc16bc97b05d8923`.
Source artifact: `10587885892`, run `35453272406`.
The extracted and locally indexed tree is `88bf0c2d3b76b65ca160c1512359e395839dc351`,
matching GitHub. Local candidate commits are not claims that the remote branch changed.

## Retained baseline failures

| Job | Observed result | Action / status |
| --- | --- | --- |
| JVM | 425 tests, zero failures/errors/skips in downloaded XML | Do not confuse this with complete build verification. |
| Static analysis | ktlint: one blank line in `StudioPalette.kt`; Detekt: nested blocks in `StudioColorSchemeTest` and `StudioPaletteChecks` | Corrected structure without removing cases/assertions or suppressing rules. Must rerun tools. |
| API 36 tablet | `BrushStudioUiTest.selectingAndUsingAPresetPublishesExactlyOnce`: Drawing pad text overflow | Replace baseline-constrained text tabs with content-measured tabs. Retain and expand overflow assertions. Device confirmation pending. |
| API 35 / 16 KB | Native SIGSEGV during `SavedBrushUiTest.savedRowsCanBeRenamedAndDeleteRequiresConfirmation` | Unresolved; do not mark infrastructure-only or disable JIT. |
| API 26, API 36, API 35 independent repeat, API 36 / 16 KB | Job success | Baseline results only, not evidence for a new revision. |

Artifacts: static reports `10587781297`; tablet `10587354224`; failing API 35 `10587471951`.
Baseline run: https://github.com/cheshmakzanoon-ops/Experiment-2/actions/runs/35453272406

The API 35 crash is at 16:01:43 UTC, process/thread 4508, page size 16384, null instruction
pointer. Frames include `SuspendAnimationKt.animate`, `Animatable.runAnimation` and `MutatorMutex`.
The log does not establish an application defect or an emulator defect. A separate Google Play
services process also crashed earlier, which is environmental context, not exoneration of the app.
No observed `Profile Saver` abort message occurs in this app's retained crash stack. Do not equate
all native crashes or all API 35 failures with PR #34's older diagnosis.

## Source divergence

PR #34 (`studio-parity-20260919`, head `0fbc29ed8d02637b53466c50b8c535acbdcc23b7`)
remains open and unmerged, and GitHub reports it unmergeable. Its affine-transform and text-session
work is not part of this main snapshot. Preserve that work and reconcile it deliberately; never
replace current main with that older branch wholesale or describe its features as already shipped.

## Executed locally

- Kotlin production palette code and shared checks: six groups, 24 combinations, 1,000 generated seeds.
- Python artifact/verifier suite: 30 tests passed.
- `git diff --check` passed.
- Gradle attempted but stopped before configuration at `UnknownHostException: services.gradle.org`.

## Required before promotion

Run the exact candidate through JVM tests, ktlint, Detekt, debug/release lint, APK/AAB builds,
artifact checks, all six existing device jobs and minified launch. Preserve failures and capture
actual tablet/compact, light/dark, enlarged-text results. The native crash needs reproduction or
stronger diagnosis; unchanged successful retries alone do not establish its root cause.

Inspect label bounds and actual screenshots. Content-measured tabs are a candidate repair; no new
screenshot was generated by local Android execution. Existing artifact captures describe the base.
No physical stylus, long-session, low-memory or artist acceptance has been completed here.

## Local brush-navigation/dynamics candidate

New source: `BrushAttribute.kt`, `BrushSettingsWorkspace.kt`; refactored
`AdvancedBrushSettingsPanel.kt` and hoisted attribute selection in `BrushStudioDialog.kt`.
All pre-existing slider labels are retained. The original `BrushDynamicsTest.kt` is byte-identical
to the base; new regression wrappers are in the separate `BrushAttributeDynamicsTest.kt`.

Seven new shared check groups exercise all ten attribute routes, size/opacity speed contracts,
source-alpha preservation, pressure-curve colour response, real rendered footprint/alpha/hue and
deterministic independent buffers. Seven existing Brush Studio check groups also pass. The test
fixtures use the production rasterizer and parameter implementation, not substitute algorithms.

Five new Android tests cover compact attribute selection, retained values, only the matching
parameter changing, group persistence across tabs, staged Use/Cancel and larger target sizes.
They have not been executed in this container. No claim of a new Android screenshot, physical
stylus result, whole-app accessibility audit or closed native-crash diagnosis is made.

The standalone probe uses Kotlin 1.9.0 and compile-only adapters for missing serialization
annotations, Android Color.BLACK and an unused Compose Color constructor signature. It does not
exercise generated serialization or replace the repository's Gradle/JUnit suite. Kotlin PSI parsing
checks source syntax only; it does not check Android types or Compose compiler constraints.
